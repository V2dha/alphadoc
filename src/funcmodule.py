"""
funcmodule.py shows how to define a simple (useless) function that __main__.py can import and invoke.
"""
def my_function(text_to_display):
    print('text from my_function :: {}'.format(text_to_display))
